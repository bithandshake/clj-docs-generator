
(ns docs.detect.engine
    (:require [candy.api :refer [return]]
              [io.api    :as io]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn detect-layers!
  ; @param (map) options
  [options])
